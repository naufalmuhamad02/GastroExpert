<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GastroExpert" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Tema tanpa Action Bar -->
    <style name="Theme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item> <!-- Opsional: Untuk mode fullscreen -->
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- Opsional: Transparan status bar -->
    </style>

    <style name="Theme.GastroExpert" parent="Base.Theme.GastroExpert" />
</resources>